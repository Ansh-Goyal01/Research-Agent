# --- caches (regenerated automatically; never commit) ---
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# --- secrets and machine-local config ---
.env
config.py

# --- runtime artifacts (regenerated by a pipeline run) ---
state/
logs/
outputs/

# --- the archived fabricating run: forensic evidence + test fixture.
# Kept out of git for size, but DO NOT DELETE from disk; tests read it. ---
baseline-golden/

# --- editor / OS / tooling ---
*.bak
.DS_Store
.claude/
